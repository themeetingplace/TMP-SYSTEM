# 卡片內容呈現 redesign audit (2026-06-15)

> 範圍：聚空間 PMS 系統內所有「row-card 型資訊卡片」(手機 ≤ 600px 自動由 data-table 轉成卡片) 的內部排版審閱。
> 目的：解決用戶反應「資訊標頭跟內容太分散」、視覺密度低、眼睛要左右大跳的問題。
> 重點：**不寫 code，提供 2~3 個有 ASCII mockup 的選項，讓用戶選一套再實作。**

---

## TL;DR

1. 全系統 7 個 row-card 變體 (物件 / 合約 / 帳務已結 / 房租查帳 / 租客 / 維修 / 帳號)，**手機版全部走同一條卡片化規則** (`css/style.css:6556-6650`)：label 靠左、value 靠右、中間大量 white space。這是「太分散」的根因。
2. 桌面版正常 (橫向資訊密度夠)，**痛點集中在手機**。
3. 問題不是「卡片該變窄」而是「卡片內 row 數太多 + 每 row 只塞 1 個資訊點」。當前實作把 5~9 個 td 一比一映射成 5~9 列 key-value，太鬆。
4. 建議：**主推「方案 A: Stacked-2-col 緊湊雙欄」** — 每 row 可塞 2 個 metadata (label 細灰字在上、值在下)，行高省一半，眼睛掃描順向縱讀不再左右跳。次推方案 C 用在「金額為王」的合約 / 帳務卡。
5. 風險低：CSS 只動 `@media (max-width: 600px)` 那塊 + 加一些 `data-card-style` hook，桌面 0 影響。

---

## 1. 系統內 row-card 盤點

| # | 卡類 | 檔案 | 桌面 td 數 | 手機列數 | 標頭/內容呈現 | 痛點等級 |
|---|---|---|---|---|---|---|
| 1 | 物件管理 (床位) | `js/views/properties.js:136-163` | 6 | 6 | 物件資訊(含 area+床位+房型 stack) / 狀態 / 租金(含 subtitle) / 租客 / 合約(含 subtitle) / 操作 | ⭐⭐⭐⭐⭐ (用戶截圖案例) |
| 2 | 合約管理 | `js/views/contracts.js:214-242` | 7 | 7 | 合約資訊(id+物件) / 租客 / 租金(含 term subtitle) / 起始日 / 到期日(含倒數) / 狀態 / 操作 | ⭐⭐⭐⭐⭐ |
| 3 | 帳務 - 已結帳目 | `js/views/finance.js:116-137` | 9 | 9 | 日期 / 館別 / 類別 / 項目(含期間/物件 sub) / 實收(含原價) / 折扣 / 付款 / 備註 / 操作 | ⭐⭐⭐⭐⭐ (最多列) |
| 4 | 房租查帳 (待結) | `js/views/unsettled.js:78-120` | 8 | 8 | checkbox / 方向 / 帳單(id+館+類型 stack) / 對象 / 金額(含已收/折扣) / 應結日(含逾期) / 末5碼 / 操作 | ⭐⭐⭐⭐ (用戶 6/13 截圖) |
| 5 | 租客清單 | `js/views/tenants.js:39-78` | 6 | 6 | 租客資訊(頭像+姓名+email) / 聯絡方式 / 居住狀態(物件+badge) / LINE 綁定 / 緊急聯絡人 / 操作 | ⭐⭐⭐ |
| 6 | 維修 | `js/views/maintenance.js:25-71` | 6 | 6 | 工單資訊(id+物件) / 報修內容(問題+回報人) / 回報時間(日期+幾天前) / 狀態 / 費用 / 操作 | ⭐⭐⭐ |
| 7 | 帳號管理 | `js/views/admin-users.js:55-72` | 5 | 5 | Email / 顯示名稱 / 角色 / 新增日期 / 操作 | ⭐⭐ |

通用機制：
- `js/utils/tableDataLabels.js` 把 thead 的文字自動寫到 td 的 `data-label`
- `css/style.css:6556-6650` 在 ≤ 600px 把 table 拆成 block，每個 td 變一列，`td::before { content: attr(data-label); }` 顯示 label 靠左，td 內容靠右 (`justify-content: space-between`)
- 第一個 td 當「卡片標題」(`font-weight: 600 + border-bottom`)，最後一個 td 當「操作列」(`border-top + flex-end`)

---

## 2. 痛點具體化 (按卡類)

### 2.1 物件管理 (床位卡) - 用戶截圖原案例

**HTML 結構** (`properties.js:136-163`)：6 個 td 對應 6 個 thead → 手機變 6 列卡片。

```
┌─────────────────────────────────────────────────┐
│ 古亭1館                       ← td:first-child  │
│ 床位 R1-A                    (column 排，沒 label)│
│ 男生 2 人房                                      │
│ ─────────────────────                            │
│ 狀態                            待租             │
│ 租金                  $9,500 ↙ 每月租金        │  ← 內部 stack (rent + subtitle)
│                                                  │     被卡片化規則 flex 拉到右邊
│ 目前租客                          --             │
│ 合約                              --             │
│ ─────────────────────                            │
│       操作       [👁]   [✏]                      │
└─────────────────────────────────────────────────┘
```

**具體痛點**：
1. **狀態、租客、合約三列只有單一短值**（`待租`、`--`、`--`）— label 靠左、值靠右，中間 200~250px 空白 (`properties.js:144-151`)。
2. **租金那列又把 `每月租金` subtitle 用 `<div>` 堆疊** (`properties.js:147-148`)，但卡片化模式 `td` 是 `display: flex; justify-content: space-between`，於是 `$9,500` 跟 `每月租金` 兩行都被擠到最右邊 → 既有「分散感」又有「右側塞兩行」的不平衡感。
3. **首列床位資訊** 一共塞了「館名 / 床位編號 / 房型」三行 (`properties.js:139-143`)，這部分還算對齊；但下面 5 列又零散 → 整張卡片變成「上面緊、下面散」。
4. **5 列只用 6 個資訊點**，密度極低，卡片高度約 280~320px。

### 2.2 合約管理 (合約卡)

**HTML 結構** (`contracts.js:214-242`)：7 td → 手機 7 列。

```
┌─────────────────────────────────────────────────┐
│ C001    續自 C000                                │
│ 聚空間 - 松山館 R1-A                             │
│ ─────────────────────                            │
│ 租客                          王小明             │
│ 租金                $18,000 ↙ 1 個月期           │
│ 起始日                      2026-06-01           │
│ 到期日                      2026-07-01 ↙剩 16 天 │  ← 內部 stack 又被靠右
│ 狀態                          [進行中]           │
│ ─────────────────────                            │
│       操作       [續] [退] [⏸] [👁] [✏] [⬇] [✉] [🗑] │  ← 操作列爆滿
└─────────────────────────────────────────────────┘
```

**具體痛點**：
1. **租客 / 起始日 / 狀態 三列又是單一短值**，左右拉開最嚴重 (`contracts.js:222, 227, 235`)。
2. **租金的 subtitle (`1 個月期`) 跟到期日的倒數 (`剩 16 天`)** — 桌面是 1 個 td 內上下 stack，到了手機被 `space-between` 推到 td 最右側 → 變成「右側兩排小字 + 左側 label」這種詭異格式 (`contracts.js:223-226, 228-234`)。
3. **操作列 7~10 個按鈕** (含決策三鍵 + 5 個標準鍵)，手機卡片化模式 `flex-end` 讓它們都靠右擠成一行，超出寬度就 wrap，影響視覺平衡 (`contracts.js:236-240`)。
4. 卡片高度 ~360px，密度跟物件卡一樣低。

### 2.3 帳務管理 - 已結帳目 (finance.js)

**HTML 結構** (`finance.js:116-137`)：**9 td** → 手機 9 列，最高。

```
┌─────────────────────────────────────────────────┐
│ 2026-06-10                  ← 第一格做卡片標題   │
│ ─────────────────────                            │
│ 館別                          松山館             │
│ 類別                          [房租]             │
│ 項目                          王小明             │  ← 桌面 stack 的 propertyText 又被靠右
│                  聚空間 - 松山館 R1-A             │
│ 實收/實付                  +$18,000              │
│                  原價 $19,000                     │  ← 桌面是同一格上下 stack
│ 折扣                  -$1,000                    │
│                  季繳優惠                         │
│ 付款                          匯款                │
│ 備註                  6 月房租尾款                │
│ ─────────────────────                            │
│       操作       [👁] [✏] [🗑]                    │
└─────────────────────────────────────────────────┘
```

**具體痛點**：
1. **9 個 td 全部單獨成列**，卡片高度爆炸 ~400px+，密度最低。
2. **項目 / 實收 / 折扣三個 td 都有內部 stack** (主值 + 補充說明)，全部被 `space-between` 推右側 → 「右側兩行 + 左側 label」結構重複 5 次 (`finance.js:118-127`)。
3. 日期當卡片標題，但日期不是用戶閱讀的首要錨點 — **金額/類別才是**。 (`finance.js:118`)
4. **`館別` 跟 `類別`** 都只有 2~4 字，跟值之間 300px 空白。

### 2.4 房租查帳 (待結卡) - 用戶 6/13 截圖案例

**HTML 結構** (`unsettled.js:78-120`)：8 td → 手機 8 列。

```
┌─────────────────────────────────────────────────┐
│ ☐                          ← checkbox td 變獨立卡頭(不適合)│
│ ─────────────────────                            │
│ 方向                          [應收]             │
│ 帳單                  INV-2026-06-001            │  ← 桌面 stack (id+館+類型)
│                  松山館 · 房租                    │     被靠右
│ 對象             王小明 · 松山館 R1-A             │
│ 金額                  $18,000                    │
│              已收 $5,000 / 應收 $18,000          │  ← 三行內部 stack
│              (折 $1,000)                          │
│ 應結日                  2026-06-10               │
│                          [逾期]                   │  ← align-self: flex-start 不生效
│ 末5碼                  [⚠ 末5碼 12345]           │
│ ─────────────────────                            │
│       操作       [核對結帳] [🔔] [✏] [🗑]         │
└─────────────────────────────────────────────────┘
```

**具體痛點**：
1. **checkbox 變獨立卡片標題**，卡片頂部第一行只有一個空 checkbox → 視覺很怪 (`unsettled.js:80`)。
2. **金額那列有 2~3 行內部 stack**（已收/應收/折扣），手機被 `space-between` + `align-items: flex-start` 整批推到右側，左側 `金額` label 像迷你一筆 (`unsettled.js:89-95`)。
3. 8 列只用 ~7 個資訊點，整卡 ~380px 高。
4. **「逾期」badge 用 `align-self: flex-start`** 想靠左，但 `td` 已經 `text-align: right + flex-end`，這 inline style 在卡片化模式幾乎無效 (`unsettled.js:99`)。

### 2.5 租客清單

**HTML 結構** (`tenants.js:39-78`)：6 td → 手機 6 列。

```
┌─────────────────────────────────────────────────┐
│ (👤) 王小明                  ← 桌面是頭像+姓名+email│
│      wang@example.com                            │
│ ─────────────────────                            │
│ 聯絡方式                    0912-345-678         │
│ 居住狀態        聚空間 - 松山館 R1-A             │
│                          [居住中]                 │
│ LINE 綁定          [✓ 已綁定] · 王小明           │
│ 緊急聯絡人        王媽媽 (0911-222-333)          │
│ ─────────────────────                            │
│       操作       [👁] [✏] [📞] [🗑]               │
└─────────────────────────────────────────────────┘
```

**具體痛點** (中度)：
1. 頭像在卡片化時，第一個 td 變 `flex-direction: column`，**40×40 圓形頭像縮到中間放大**或破版 — 取決於 css 細節，視覺很尷尬 (`tenants.js:43-50`)。
2. 聯絡方式、緊急聯絡人 — 單一短值左右拉開，分散感明顯 (`tenants.js:52, 60`)。
3. 「居住狀態」是 stack (物件 + 狀態 badge)，又是兩行被推右 (`tenants.js:53-58`)。
4. 卡片高度尚可 (~260px)，但「聯絡方式 / LINE / 緊急聯絡人」三列其實可以橫向排在一起。

### 2.6 維修

**HTML 結構** (`maintenance.js:25-71`)：6 td → 手機 6 列。

```
┌─────────────────────────────────────────────────┐
│ MAINT-001                                        │
│ 聚空間 - 古亭1館 R1-A                            │
│ ─────────────────────                            │
│ 報修內容              冷氣不冷會滴水             │  ← stack: issue + reporter
│                   回報人: 王大明                  │
│ 回報時間              2026-06-10                 │
│                          3 天前                   │
│ 狀態                          [進行中]           │
│ 維修費用                          $2,500         │
│ ─────────────────────                            │
│       操作       [▶] [✓] [👁] [✏] [🗑]            │
└─────────────────────────────────────────────────┘
```

**具體痛點** (中度)：
1. 狀態 / 維修費用 兩個單值列分散 (`maintenance.js:45-46`)。
2. 報修內容若 issue 很長 (描述完整) 會被推到右側 + 換行，左側 `報修內容` 五個字看起來像孤兒 — 因為 issue 自身需要 left-align (內文型，不是右靠值)。
3. 整體還好，問題輕。

### 2.7 帳號管理

**HTML 結構** (`admin-users.js:55-72`)：5 td → 手機 5 列。

```
┌─────────────────────────────────────────────────┐
│ owner@gmail.com  [你]                            │
│ ─────────────────────                            │
│ 顯示名稱                          老闆           │
│ 角色                          [👑 Owner]         │
│ 新增日期                      2026-01-15         │
│ ─────────────────────                            │
│       操作                  [🗑 移除]            │
└─────────────────────────────────────────────────┘
```

**具體痛點** (輕度)：
1. 顯示名稱 / 角色 / 新增日期 三列都單值，分散感存在但因為這頁本來資料就少 (5 個欄位 → 4 列)、用戶停留時間短，影響相對最小。
2. 這頁可以套用相同方案，但 ROI 最低。

---

## 3. 改善方案 (3 個選項)

> 共同原則：**只動 `@media (max-width: 600px)` 規則**，桌面完全不變。設計 token (`--text-*`、`--color-*`) 不換。modal 內部不動。

---

### 3.1 方案 A: Stacked-2-col 緊湊雙欄 **(推薦主套用)**

**核心改變**：
- **label 改成「上方細小灰字 (caption)」**，值放在下方，每個 metadata 是「兩行迷你 block」。
- **卡片內 metadata 區塊用 `grid-template-columns: 1fr 1fr` (or `repeat(auto-fit, minmax(140px, 1fr))`) 兩欄並排**。
- 標題列 + 操作列依然獨立，但中間 metadata 從垂直 5~9 列壓縮成 2~3 列雙欄。

**ASCII mockup (床位卡)**：

```
┌────────────────────────────────────────┐
│ 古亭1館 · R1-A                          │  ← 標題列 (左對齊大字)
│ 男生 2 人房                             │     subtitle 小字
│ ──────────────────────────              │
│ 狀態           租金                      │  ← 雙欄 metadata
│ 待租           $9,500/月                 │     label 在上灰小字
│                                          │     值在下黑大字 + 緊密
│ 目前租客       合約                      │
│ —              —                         │
│ ──────────────────────────              │
│              [👁 查看]  [✏ 編輯]         │  ← 操作列右靠
└────────────────────────────────────────┘
```

**ASCII mockup (合約卡)**：

```
┌────────────────────────────────────────┐
│ C001 · 王小明        [進行中] [✅要續]  │  ← id + 租客 + 狀態 chip 同列
│ 聚空間 - 松山館 R1-A                    │     物件 subtitle
│ ──────────────────────────              │
│ 租金                 期間                │
│ $18,000/月           1 個月期            │
│                                          │
│ 起始日               到期日              │
│ 2026-06-01           2026-07-01          │
│                      ⏰ 剩 16 天          │
│ ──────────────────────────              │
│ [續租] [退租] [⋯ 更多]                  │  ← 主 CTA + kebab 收常用
└────────────────────────────────────────┘
```

**ASCII mockup (帳務已結)**：

```
┌────────────────────────────────────────┐
│ +$18,000 · 房租                  [收入]  │  ← 金額為主標
│ 2026-06-10 · 松山館                      │     日期+館別 subtitle
│ ──────────────────────────              │
│ 項目                 折扣                │
│ 王小明               -$1,000             │
│ R1-A 租期 06/01~06/30 季繳優惠           │
│                                          │
│ 付款                 備註                │
│ 匯款                 6月房租尾款         │
│ ──────────────────────────              │
│              [👁]  [✏]  [🗑]            │
└────────────────────────────────────────┘
```

**適合的卡類**：
- ⭐ 最佳：物件 / 租客 / 維修 / 帳號管理 (中等資訊量，每欄都有意義)
- 良好：合約 / 房租查帳 / 帳務已結 (高資訊量也能 fit，3 row × 2 col = 6 metadata 剛好)

**Pros**：
- 資訊密度直接翻倍 (5 列 → 2~3 列)
- 每個 metadata 內部「label 在上、值在下」自然成迷你 block，眼睛縱讀順向，不再左右跳
- 灰小 caption + 黑大值 是常見 mobile pattern (Notion / Stripe / Linear 都類似)
- 卡片高度從 ~300-400px 壓到 ~180-240px，列表一畫面看得到 3~4 張

**Cons**：
- 雙欄要求每欄寬度 ≥ 140px，極窄手機 (≤ 340px e.g. iPhone SE) 可能要 fallback 單欄
- thead 自動 data-label 機制不夠用 — 需要在 view 寫死 td 順序對應的格子位置 (或加 CSS Grid area name)
- 部分 td 內已經自己 stack (e.g. 租金 + subtitle) 要拆 → 改動最大

**工作量**：**M** (3~5 個 view 動 HTML 結構 + CSS 增加 ~80 行)

---

### 3.2 方案 B: Inline-tight 內聯緊貼

**核心改變**：
- label 跟 value 改成同一行貼緊 (`label: value` 像聊天 metadata)，多個 metadata 在同一行用「·」分隔
- 沒有左右兩端對齊，沒有大片空白
- 適合 metadata 短、需要快速瀏覽的場景

**ASCII mockup (床位卡)**：

```
┌────────────────────────────────────────┐
│ 古亭1館 · R1-A · 男生 2 人房            │  ← 標題
│ ──────────────────────────              │
│ 狀態：待租 · 租金：$9,500/月            │  ← inline tight
│ 租客：— · 合約：—                       │
│ ──────────────────────────              │
│              [👁 查看]  [✏ 編輯]         │
└────────────────────────────────────────┘
```

**ASCII mockup (帳號管理)**：

```
┌────────────────────────────────────────┐
│ owner@gmail.com  [👑 Owner] [你]        │
│ 顯示名稱：老闆 · 加入：2026-01-15       │
│                              [🗑 移除]   │
└────────────────────────────────────────┘
```

**適合的卡類**：
- ⭐ 最佳：帳號管理 / 維修 (欄位少、值短)
- 良好：租客 (聯絡 metadata 適合 inline)
- 不適合：合約 / 帳務 (值有金額需要強調，跟 label 拉同樣字級會弱化)

**Pros**：
- 卡片最矮 (~140-180px)，列表一畫面可看 5~6 張
- 程式碼最少 — 直接把現有 td 內容改成 `<span>label：</span><strong>value</strong>` + `·` 連接
- 不需要 grid，極窄手機也安全

**Cons**：
- **金額會變平**，「實收 $18,000」跟 label 同字級時無視覺權重
- 多欄連在一起資訊密度極高，但**掃描錨點變少** — 找特定欄位要逐字讀
- 用戶截圖之前抱怨「分散」，這方案矯枉過正變「擠」

**工作量**：**S** (CSS 改 ~30 行 + view 微調 ~每個 view 10 行)

---

### 3.3 方案 C: Title-prominent + chip metadata (金額為王)

**核心改變**：
- 卡片頂部把**最重要的數字 / 狀態**放大置中或左大字 (「英雄區」)
- 其他 metadata 變成下方 chip 小方塊列 (像 tag pill)
- 適合「金額導向」的卡片 (合約 / 帳務 / 房租查帳)

**ASCII mockup (合約卡)**：

```
┌────────────────────────────────────────┐
│  $18,000/月 · 1 個月期      [進行中]    │  ← 英雄區：金額大字+狀態
│  ────────────────────────              │
│  C001 · 王小明                          │  ← id + 租客
│  聚空間 - 松山館 R1-A                   │
│                                          │
│  [📅 06/01~07/01]  [⏰ 剩 16 天]        │  ← chip 列
│  [📝 續自 C000]   [✅ 要續租]            │
│  ────────────────────────              │
│  [續租] [退租] [⋯ 更多]                 │
└────────────────────────────────────────┘
```

**ASCII mockup (房租查帳)**：

```
┌────────────────────────────────────────┐
│  $18,000              [⚠ 逾期 3 天]    │  ← 餘額大字 + 逾期 badge
│  ────────────────────────              │
│  INV-2026-06-001                        │
│  王小明 · 松山館 R1-A                   │
│                                          │
│  [💰 已收 $5,000]  [🏷 折 $1,000]      │  ← chip
│  [📅 應結 06/10]   [⚠ 末5碼 12345]     │
│  ────────────────────────              │
│  ☐  [核對結帳] [🔔 催]                  │
└────────────────────────────────────────┘
```

**ASCII mockup (帳務已結)**：

```
┌────────────────────────────────────────┐
│  +$18,000                    [收入]     │
│  房租 · 2026-06-10                      │
│  ────────────────────────              │
│  王小明 · 松山館 R1-A                   │
│  租期 06/01 ~ 06/30                     │
│                                          │
│  [🏷 折扣 -$1,000 (季繳優惠)]            │
│  [💳 匯款]  [📝 6月房租尾款]            │
│  ────────────────────────              │
│              [👁]  [✏]  [🗑]            │
└────────────────────────────────────────┘
```

**適合的卡類**：
- ⭐ 最佳：合約 / 帳務已結 / 房租查帳 (金額是首要 scan 目標)
- 不適合：物件 / 租客 / 維修 / 帳號 (沒有單一「英雄數值」)

**Pros**：
- 用戶最關心的數字 (金額 / 倒數 / 狀態) 第一眼就抓得到
- chip 列視覺活潑，每個 chip 等於一個「貼紙標籤」感
- 對「待結帳款」這種 actionable 列表特別有效 — 一眼看金額排序

**Cons**：
- chip 設計成本高，要管理 icon、顏色、字級
- 三個方案中改動最大 (英雄區 + chip 區是新元件)
- 在純資訊型 (物件 / 租客) 套用反而矯枉 — 不是每張卡都該有「主角數字」

**工作量**：**L** (3~4 個 view 大改 HTML + CSS 新增 ~150 行 chip 元件樣式)

---

## 4. 推薦

### 全面套用 → **方案 A: Stacked-2-col 緊湊雙欄**

理由：
1. **覆蓋率最高** — 7 個卡類 100% 都能套，物件 / 租客 / 維修 / 帳號完美，合約 / 帳務 / 房租查帳 至少不會比現在差。
2. **資訊密度提升一倍** 直接解決用戶痛點，但**不過度密集** (方案 B 那種程度)。
3. **語意正確** — label 在上灰小字、值在下強調，是現代 mobile UI 的標準慣例 (Settings App、Stripe、Notion 都是這個 pattern)，用戶看到不會陌生。
4. **改動量適中** (M)，可控、可回滾。

### 局部加強 → **方案 C 用在金額 3 卡**

合約管理 / 帳務已結 / 房租查帳 這 3 個「金額為王」的卡片，**在方案 A 的骨架上加一個英雄區** (頂部把金額放大成 1.5~2rem 字級)，下方 metadata 區照舊用方案 A 的雙欄。這樣每張金額卡都有強錨點，又不失方案 A 的統一感。

### 不建議套用方案 B

方案 B 雖然簡單，但「inline tight + 用·分隔」會讓金額弱化、metadata 失去掃描錨點 — 跟現在「分散」相反方向過頭。**只在帳號管理頁** (這頁本來資料少) 可以考慮 — 但 ROI 太低、不一致反而擾亂用戶心智模型。

---

## 5. 影響範圍 / 風險

| 項目 | 影響 | 風險 |
|---|---|---|
| 桌面版 | 0 改動 (所有 CSS 改動在 `@media (max-width: 600px)` 內) | 無 |
| modal 內部 | 0 改動 (已 redesign 過、不在此範圍) | 無 |
| 表格卡片化通用機制 (`tableDataLabels.js`) | 需要保留 (auto data-label 仍是基礎) | 低 — 方案 A 額外用 grid-area 或 nth-child 指定欄位位置，data-label 仍是 fallback |
| 設計 token | 不換，沿用既有 `--text-*` / `--color-*` | 無 |
| iPhone SE / Galaxy A (≤ 340px) | 雙欄可能太擠 | 需要在 `@media (max-width: 360px)` 加 fallback 單欄 |
| 既有 view 程式碼 | 需修改 td 內部 stack 結構 (e.g. `properties.js:147-148` 把租金 subtitle 拆出) | 中 — 桌面要保持原樣，會引入「桌面 1 種 / 手機 1 種」雙渲染或用 CSS hide/show |
| 卡片化「第一個 td 變標題」規則 | 方案 A 可能需要新規則 (前 1~2 個 td 是標題區、之後進 grid 雙欄、最後 td 是操作) | 中 — CSS specificity 要管理好 |
| `unsettled.js` checkbox td | 在方案 A 要從「卡片標題」位置移到右上角 (像 email inbox) | 低 — 寫 `td:has(input[type=checkbox])` 特例 |

---

## 6. 工作量級

| 方案 | 涉及 view 檔案 | 涉及 CSS | 程式碼變動量 (粗估) | 時間估 |
|---|---|---|---|---|
| **A. Stacked-2-col** | 7 個 view (物件/合約/帳務/查帳/租客/維修/帳號) | `style.css:6556-6650` 重寫 + 新增 ~80 行 | view 各 ~30-60 行 (主要是把 td 內部 stack 拆掉或加 grid hint) + CSS ~80 行 | **M (1~1.5 天)** |
| **B. Inline-tight** | 7 個 view 微改 | `style.css:6556-6650` 改 ~30 行 | view 各 ~15 行 + CSS ~30 行 | **S (4~6 小時)** |
| **C. Title + chip** | 3 個金額 view (合約/帳務/查帳) 大改 + 4 個沿用 | `style.css` 新增 chip 元件 ~150 行 | view 各 ~80-100 行 + 新 chip CSS | **L (2~3 天)** |
| **A + C 混搭 (建議)** | 7 個 view 都套 A，金額 3 卡加英雄區 | `style.css` ~120 行 (A 80 + 英雄區 40) | view 共 ~350 行 + CSS ~120 行 | **M-L (1.5~2 天)** |

---

## 附錄：CSS 規則對照 (供實作時參考)

| 規則 | 檔案位置 | 用途 | 方案 A 是否動 |
|---|---|---|---|
| `@media (max-width: 600px)` row-card 卡片化 | `css/style.css:6556-6650` | 把 data-table 變卡片清單 | ✅ 主要改動點 |
| `td::before { content: attr(data-label) }` | `css/style.css:6605-6614` | label 從 thead 自動帶出靠左 | ✅ 拆掉，改成「label 在 td value 之上」 |
| `td:first-child` 當卡片標題 | `css/style.css:6616-6627` | 第一個 td 加 border-bottom + 大字 | ✅ 沿用標題概念，可能加 `td:first-child + td:nth-child(2)` 一起當標題 |
| `td:last-child` 當操作列 | `css/style.css:6630-6640` | 最後 td 加 border-top + flex-end | ✅ 沿用 |
| `td` flex space-between | `css/style.css:6595-6604` | 中間值的對齊規則 (痛點根源) | ✅ **改成 column flex + label 細灰字 caption** |
| `.contract-row-card` (方案 B 卡片化) | `css/style.css:5538-5811` | 已存在但 contracts.js 沒在用 (用 tr) | 不動 (孤立未引用) |
| `tableDataLabels.js` 自動 data-label | `js/utils/tableDataLabels.js` | scan thead 給 td 加屬性 | 保留 (做 fallback) |

---

**報告完。**
