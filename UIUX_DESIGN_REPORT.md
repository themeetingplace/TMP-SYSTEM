# 聚空間 BMS — 設計改善路線圖

> 給 KI 老闆的 UI/UX 提案
> 整合視覺 / 互動 / 資訊架構 / 無障礙 四份審查
> 日期：2026-05-29

---

## 開場

這份報告把四位設計師獨立看完系統後寫的 53 條 findings，去重整合成 **38 個改善項目**，按「KI 每天怎麼用這個系統」的脈絡分章節，再標上嚴重度。

不談程式怎麼寫，只談「**該做什麼、為什麼**」。實作方法後面交給工程細談。

**閱讀順序建議**：先看最後的「設計師說的話」抓總方向，再回頭看「建議優先做的 5 件事」決定本季預算，剩下章節當作 backlog 慢慢做。

---

## 📊 概覽

### 嚴重度分布

| 等級 | 數量 | 意涵 |
|---|---|---|
| 🔴 P0 阻礙使用 | 5 | 真的會卡住 KI 完成任務、或造成資料事故 |
| 🟡 P1 摩擦明顯 | 16 | 能用但天天踩雷，長期累積離職率 |
| 🟢 P2 體驗改善 | 13 | 做完會明顯感受「變舒服了」 |
| ⚪ P3 拋光 | 4 | 細節美感、品牌一致性 |

### 四面向分布

| 面向 | 件數 | 主要痛點 |
|---|---|---|
| 視覺設計 | 12 | 對比不足、字級太小、樣式系統碎裂 |
| 互動設計 | 11 | 危險操作無護欄、缺少 undo、表單太長 |
| 資訊架構 | 9 | 帳務 4 個子頁職責重疊、搜尋無法直達 |
| 無障礙 / 響應式 | 6 | Modal 無 ARIA、手機 sidebar 不收、focus 不見 |

---

## 一、第一印象（登入後到 Dashboard）

KI 早上打開系統的前 10 秒決定一整天心情。目前進來的視覺體驗是「乾淨但沒重點」。

### 🔴 P0｜全域搜尋找到結果，但點下去只回到列表頁，沒打開那筆

- **痛點**：KI 搜「王大明」找到 5 筆結果，點任何一筆只跳到 list，還要在列表頁再搜一次「王大明」。117 床 / 91 合約的規模下，每次都搜兩次。
- **改法**：搜尋結果直接呼叫對應的 detail modal（合約 → 合約詳情、租客 → 租客詳情），帳單則跳該頁 + 自動 scroll + highlight。
- **證據**：`js/components/topbar.js:30-41`
- **工作量**：M

### 🟡 P1｜Dashboard 4 張 metric 卡視覺權重相同，KI 不知道該先看哪個

- **痛點**：「物件已租 / 待簽約 / 待維修 / 本月租金」四卡平均分配。眼睛第一眼會掃到右上角搜尋框的橘色 focus ring，而不是該關注的數據。
- **改法**：改成「1 大主卡 + 3 小副卡」結構，主卡放當天最需行動的（如：本月待結帳款金額），有 > 0 的待處理項加紅點或脈動。
- **證據**：`js/views/dashboard.js:182-228`
- **工作量**：M

### 🟡 P1｜Dashboard 待辦點「決策 / 查看」也只跳列表，不開那筆

- **痛點**：跟全域搜尋同一個 IA 缺陷。dashboard 應該是「行動入口」，現在點完待辦還要再找一次。3 筆待辦 × 每天 = 多按 6 次無謂的滑鼠。
- **改法**：todo item 帶上 entity id，點擊時直接開該筆 modal（不換頁），處理完關 modal 還在 dashboard 繼續處理下一筆。
- **證據**：`js/views/dashboard.js:287`（link 只有 'contracts' / 'unsettled'，沒帶 id）
- **工作量**：M

### 🟡 P1｜Dashboard 待辦寫死只顯示 3 筆，沒有「展開全部」

- **痛點**：KI 看到「合約事項只有 3 筆」會以為這就是全部，實際上可能還有 5 筆被截掉，重要決策被藏起來。
- **改法**：卡片底部加「還有 N 筆 · 看全部 →」連結；若該類別 ≥10 筆，紅色徽章提示嚴重程度。
- **證據**：`dashboard.js:57, 80, 100`（`.slice(0, 3)`）
- **工作量**：S

### 🟢 P2｜Sidebar 分組標籤過度設計（一節只放一個項目）

- **痛點**：「總覽」分組下只有「首頁」一項，「系統」分組下只有「系統設定」一項。section label 比內容還多字，反而是視覺噪音，誤導 KI「這分類下應該還有別的東西」。
- **改法**：拿掉「總覽」「系統」兩個 label，首頁直接放最上面，系統設定移到 sidebar 底部作為 utility item。
- **證據**：`index.html:39-45`
- **工作量**：S

### 🟢 P2｜Sidebar avatar「A」+ 永遠綠點線上 + 灰色「同步未啟用」順序錯置

- **痛點**：KI 只有一個帳號，「A」和「線上」沒任何資訊量，顏色卻最搶眼。真正該被注意的「同步未啟用」反而灰色被忽略。
- **改法**：avatar 用首字（KI / 中文姓），「同步未啟用」做成黃色 warning chip 讓它跳出來；別把設定藏在 hover 才出現的按鈕後面。
- **證據**：`index.html:113-132`, `style.css:1551-1597`
- **工作量**：S

---

## 二、核心日常任務（查 / 收 / 管房客房租房間）

這章是 KI 一天花最多時間的地方。findings 最密集。

### 🔴 P0｜帳務管理 4 個子頁職責高度重疊，命名混亂

- **痛點**：「總收支表 / 收支分析 / 房租查帳 / 各館收入報表」四個並列頁面，從名稱看不出差異——三者都是已結帳目的不同切片。「房租查帳」更慘：code 叫 `unsettled`、註解叫「待結帳款」、sidebar 叫「房租查帳」、頁內文案叫「租客尚未繳款」——同一概念 4 個名字。KI 想看「松山館這個月賺多少」會在三頁來回切。
- **改法**：合併成「帳務管理」單一頁面 + 內部 sub-tabs（`已結帳目 | 待結帳款 | 收支分析 | 房東報表`），共用月份切換器；命名全系統統一成「待結帳款」一個說法。
- **證據**：`finance.js:1`, `analysis.js:1`, `unsettled.js:1-3`, `reports.js:1`, `index.html:75-92`
- **工作量**：L

### 🔴 P0｜新增入住表單一次塞 15+ 欄位，無分步

- **痛點**：實際場景是「客人坐在你面前邊問邊填」，但 modal 是一個 640px 寬的長 form，欄位順序混雜（床位 → 租客 → 緊急聯絡人 → 收款全在一頁）。漏填只跳 toast 不告訴你「現在第幾步、還剩幾項」。
- **改法**：拆 3 步 wizard（① 床位+日期 → ② 租客資料 → ③ 收款），每步 progress bar；折扣/已收款等進階欄位預設摺疊。
- **證據**：`js/views/properties.js:415-686`
- **工作量**：L

### 🟡 P1｜「住房一覽」應該升級成頂層 menu，不是「物件管理」的子項

- **痛點**：住房一覽本質是跨床位 × 月份的矩陣時間軸，跟合約 / 帳務的關係比跟物件管理更近。KI 想找「下個月誰退房」憑直覺會點「合約管理」或「住房一覽」，但目前要先展開「物件管理」父節點才看得到，且父節點只有一個 child（沒省到空間）。
- **改法**：「住房一覽」升頂層 menu（icon 用 `ph-calendar-blank`），順序：物件管理 → 住房一覽 → 合約管理。
- **證據**：`index.html:49-61`, `occupancy.js:1`
- **工作量**：S

### 🟡 P1｜核對末 5 碼 modal 強制再輸入一次，繁瑣易錯

- **痛點**：客戶回報的 5 碼 readonly 顯示，房東要在第二欄重打銀行 App 看到的 5 碼。兩串字明明在面前，肉眼比對就行，再打一次反而增加打錯機率（手誤輸入「07291」而非「07219」就誤判失敗）。15 號月結那天可能要核對 30+ 筆。
- **改法**：改成「✅ 已核對相符」勾選框 + 一鍵結帳；保留輸入的話也要做「逐字比對」即時 highlight 不同位數。
- **證據**：`unsettled.js:192-235`
- **工作量**：M

### 🟡 P1｜床位 detail 顯示「合約編號」是純文字，無法直達該合約

- **痛點**：工作流是「床位 → 看現在住誰 / 哪份合約 → 看合約狀態」。租客已可點，合約卻不能點，chain 斷在中間，要回 sidebar → 合約管理 → 搜 ID → 開啟。
- **改法**：合約編號做成 button 直達合約 detail；合約 detail 內的「物件」也應反向可點回床位 detail。
- **證據**：`properties.js:399`, `contracts.js:410`
- **工作量**：S

### 🟡 P1｜編輯床位 modal 與編輯合約完全分離

- **痛點**：床位 modal 的「狀態」欄位 hint 明寫「租客 / 合約資訊請於『合約管理』修改」——KI 要記下床位 ID，切到合約管理頁，搜尋，再進去改。
- **改法**：床位 modal 加 read-only 區塊「目前合約：C089 · 王小明 · 至 6/30」+「→ 編輯此合約」一鍵跳轉。
- **證據**：`properties.js:286-289`
- **工作量**：S

### 🟡 P1｜新增報修不會自動帶入該床位的目前租客

- **痛點**：選了物件後系統明明知道現在住誰，卻要 KI 手打回報人姓名，容易打錯字導致報表搜不到。LINE Bot 報修進來時更應自動帶。
- **改法**：選 propertyName 後自動帶入該床位的 active tenant 到 reporter 欄位；維修完成時的「維修費用」欄位多加「廠商」欄位。
- **證據**：`maintenance.js:121-149`
- **工作量**：S

### 🟡 P1｜合約列表操作欄塞 9 顆 icon-only 按鈕，意思要靠記

- **痛點**：一列最多顯示：續租 / 退租 / 暫緩 / 檢視 / 編輯 / 下載 / 寄 LINE / 簽署檔 / 刪除 = 9 顆，全是 24×24 icon。要 hover 才看 tooltip；新手分不清 `paper-plane-tilt` 和 `download`，誤點「刪除」（紅垃圾桶）跟「下載」（往下箭頭）是高機率事件。
- **改法**：常用 + 文字按鈕外露（續租 / 退租 帶文字）；其餘收進「⋯」kebab menu；刪除從 row 移除，只在 detail modal 內顯示。
- **證據**：`contracts.js:107-141`
- **工作量**：M

### 🟡 P1｜送 LINE 合約連發 3 個 toast，失敗時看不出在哪步爆掉

- **痛點**：連續 toast「產生 PDF 中… → 上傳雲端… → 推送 LINE…」會疊在一起 2.5 秒消失。失敗時只說「發送失敗」，KI 不知道是 PDF 沒產出、Storage 失敗、還是 LINE API 拒絕。重試要從頭來。
- **改法**：改成 inline progress modal（單一 modal 內顯示三步驟 checklist），失敗時保留 modal 並顯示「重試此步驟」按鈕。
- **證據**：`contracts.js:548-573`
- **工作量**：M

### 🟢 P2｜住房一覽矩陣的繳費 badge (✓/◐/!) 太小，無批次標記

- **痛點**：badge 只在月份格子右上角放極小符號，掃描整館時看不到「這個月有 3 個房客還沒繳」。沒有「點 badge 直接標記已繳」捷徑，必須跳到房租查帳頁找對應 invoice。
- **改法**：月份格底色用條紋 / 角標明顯區分繳費狀態；badge 點擊跳 mini-popover「快速結帳」inline 完成。
- **證據**：`occupancy.js:70-75`
- **工作量**：M

### 🟢 P2｜住房一覽矩陣的色彩語意：圖例說「今天=綠」，實際格子是藍色

- **痛點**：圖例 swatch 是綠色，但表內今天的格子實際是藍色——完全對不上。第一次用會懷疑自己看錯。而且用藍色當「未來」也容易被誤點（多數系統藍色 = 連結色）。
- **改法**：實作與圖例對齊（今天 = 綠），「未來月份」改用中性灰，藍色保留給連結。
- **證據**：`style.css:1349-1437`
- **工作量**：S

### 🟢 P2｜矩陣表字級 0.75rem，但無法切換密度

- **痛點**：occ-table 內含 0.65rem badge，視覺擁擠；同時 finance-table 用 0.875rem 又顯得空。沒有「密度層級」概念。
- **改法**：定義 `dense` 與 `comfortable` 兩種密度，住房一覽提供切換；row 高度至少 44px 方便點擊。
- **證據**：`style.css:1332`, `style.css:2215`
- **工作量**：M

---

## 三、危險動作 / 不可逆操作

這章三個 P0 是最該優先處理的「資料事故風險」。MEMORY 裡有記載「用戶手動改過 3 次資料，重跑會覆蓋」——這類事件就是因為缺少護欄。

### 🔴 P0｜「補產缺帳單」按鈕無 dry-run，KI 不敢按

- **痛點**：按鈕 tooltip 寫「檢查所有進行中合約是否都有對應帳單，若無則補產」，但沒有預覽——KI 不知道「按下去會影響哪 N 筆合約」。對任何「批次自動產生」按鈕都有 PTSD。
- **改法**：confirm modal 先跑 dry-run，列出受影響合約清單（含 ID / 租客 / 金額），讓 KI 勾選後再執行。或改成 per-row 操作（在缺帳單的合約列加按鈕）。
- **證據**：`unsettled.js:144-146, 290-310`
- **工作量**：M

### 🔴 P0｜所有結帳 / 退租 / 續租是單行道，無 undo

- **痛點**：手滑點錯客戶 → 唯一補救是去帳務管理頁找出來，手動把 `paidAmount` 改回 0、清空 `paidDate`。對非技術背景房東是極大壓力源。
- **改法**：toast 加「↶ 復原」按鈕（5-10 秒倒數），記憶體裡保留 last 3 個操作 snapshot。Gmail 風格 undo 是業界最低標。
- **證據**：`unsettled.js:243-258`, `contracts.js:578-609`
- **工作量**：M

### 🟡 P1｜退租流程入口散在三處，操作邏輯不一致

- **痛點**：合約列表點 `ph-door-open` icon、住房一覽勾「退房」checkbox（checkbox 通常代表「選取」不是「執行」，反直覺）、合約 detail modal 卻沒退租按鈕。
- **改法**：統一改成紅色「退房」按鈕（`ph-sign-out`）；合約 detail footer 加退租次要按鈕；住房一覽改成 hover 才出現的文字按鈕。
- **證據**：`contracts.js:611`, `occupancy.js:366-370`
- **工作量**：M

### 🟡 P1｜表單必填驗證只跳 toast，不在欄位旁顯示 inline error

- **痛點**：toast 在右下角，欄位錯誤紅框在 modal 中間，眼睛要來回移動；多個欄位錯只報第一個，補完按 submit 又跳下一個。
- **改法**：欄位下方直接 render inline error 訊息，一次列出所有缺失欄位（仍 focus 第一個並 scrollIntoView）；toast 改成 summary「請補上 3 個欄位」。
- **證據**：`js/utils/ui.js:126-133`
- **工作量**：S

### 🟢 P2｜刪除合約 confirm 雖有提醒，但沒擋下有 invoice 連動的合約

- **痛點**：訊息寫「建議用退租而非刪除」，但 KI 看完還是會按下去。底下的 invoice、tenant 會變孤兒，之後帳務頁出現「合約 C089」但點不開，造成資料破窗。
- **改法**：刪除前檢查是否有相關 invoice，有就改成「無法刪除（有 N 筆相關帳款）」並提供「改為退租」按鈕。
- **證據**：`contracts.js:440-454`
- **工作量**：S

### 🟢 P2｜表單儲存規則不一致（有的跳 review 有的直接寫入）

- **痛點**：入住表單 submit 後跳 review modal、退租 modal 含摘要、編輯合約 / 新增報修卻直接寫入。KI 會養成「亂按 submit 反正有 review」習慣，遇到沒 review 的就出事。
- **改法**：統一規則——「新建 + 連動其他資料」必跳 review；單純更新單一 record 直接寫入 + toast（並可 undo，配合上面 #2）。
- **證據**：`properties.js:528`, `contracts.js:304`, `maintenance.js:121`
- **工作量**：M

### 🟢 P2｜sidebar 登出按鈕跟「主題」「帳號」橫排 + 觸控目標太小

- **痛點**：三個 28×28 按鈕橫排，最後一個是「登出」，房東在筆電 trackpad 上想點主題誤按登出 → 強迫重跑 OTP。
- **改法**：放大到 36-40px，登出按鈕與另外兩個之間加額外間隔，登出時加二次確認。
- **證據**：`index.html:124-132`, `style.css:1599-1612`
- **工作量**：S

### 🟡 P1｜沒有「儲存後繼續新增下一筆」

- **痛點**：月初一次收 8 筆轉帳對照、月中新增 3 個入住——每次都要 close → 點「新增」→ 重開 → 再選一次館別。流程斷裂感嚴重。
- **改法**：footer 加第三顆按鈕「儲存並繼續新增」（`Ctrl+Enter` 觸發），儲存後不關 modal，清空欄位但保留 sticky 預設值（館別、類型）。
- **證據**：全部 `openFormModal`（`js/utils/ui.js:90-93`）
- **工作量**：M

---

## 四、視覺一致性 / Design System

這章是系統的「健康度」——做完之後每個未來新 feature 都會自動更一致、更好維護。

### 🔴 P0｜主品牌橘 `#ff8859` 對比 3.05:1 未達 WCAG AA，卻被當文字色用

- **痛點**：sidebar 當前頁名、租客連結、metric 邊上小字都用這個橘色。中年眼睛長時間看會疲勞，亮環境下乾脆看不清。
- **改法**：保留 `#ff8859` 當「裝飾色」（icon bg、logo、按鈕底）；定義 `--color-primary-text: #c2410c` 給「文字 / 連結」用（已有多處 hardcode 這色，把慣例規範化）。
- **證據**：`css/style.css:3`, `css/style.css:286`, `css/style.css:3232`
- **工作量**：S

### 🟡 P1｜警示色 `#b8871f` 在小字 chip 上對比 3.95:1 也不夠

- **痛點**：「待核對」「即將到期」chip 是 KI 每天最該掃描的資訊，視覺優先序最高，但對比不夠時眼睛直接跳過。
- **改法**：warning 文字改 `#92660d`（≈4.6:1），背景保持淺色；或加 1px 深色 border 提升對比。
- **證據**：`style.css:13`, `style.css:2332`
- **工作量**：S

### 🟡 P1｜491 處 inline `style=""`，樣式系統實質瓦解

- **痛點**：14 個 view 檔共 491 處 inline style。要改一顆按鈕大小要改 100 個地方；新加 view 一定跟現有不一致；dark mode 完全做不了。
- **改法**：抽出 `btn-xs` / `text-meta` / `text-sub` / `muted-label` 等 utility class；優先處理 contracts.js / unsettled.js / finance.js 三個按鈕地獄。
- **證據**：`js/views/contracts.js:108-141`, `dashboard.js:280-323`
- **工作量**：L

### 🟡 P1｜同一概念有 4 套 tab 樣式

- **痛點**：`.filter-tab` / `.occ-tab` / `.settings-tab` / `.property-filter-btn` 長相完全不同。KI 學一次卻要學四次，每跳一頁要重建心智模型。
- **改法**：只留 2 種 pattern——主層級切換（底線 tab）+ 同層 filter（pill chip），其餘收歸到這兩種。
- **證據**：`style.css:2289`, `style.css:1171`, `style.css:2657`, `dashboard.js:249`
- **工作量**：M

### 🟡 P1｜Phosphor icon 的 `ph` 與 `ph-fill` 混用沒規則

- **痛點**：fill icon 視覺重量高 30%，應該保留給「高優先狀態」或「品牌」。現在 165 個 outline 混 12 個 fill 沒規律，KI 抓不到「哪個比較重要」的層級。
- **改法**：規則化——fill 只用於：(1) 品牌 logo / avatar；(2) 「已完成 / 已驗證 / 緊急」三種狀態。其他全部 line。
- **證據**：`dashboard.js:152, 267`, `index.html:29`
- **工作量**：S

### 🟡 P1｜字級階層斷層大，0.6x rem 字眾多

- **痛點**：CSS 內 8 處 0.625-0.6875rem（≈10-11px），中文 Noto Sans TC 在這尺寸筆畫會黏在一起。住房一覽矩陣表（0.75rem + 內含 0.65rem badge）KI 看一整天非常傷眼。
- **改法**：定義字級階梯（12/13/14/16/18/24/32），最小不低於 12px。中文 view 統一往上靠到 0.75rem 以上。
- **證據**：`style.css:155, 236, 955, 1298, 1374, 1478, 1651, 1816`
- **工作量**：M

### 🟢 P2｜灰階變數命名混亂，5 個近似灰白互相打架

- **痛點**：`--color-background` / `--bg-secondary` / `--bg-tertiary` / `--color-surface` / `--sidebar-item-hover-bg` 都是相近灰白。開發者只能猜，猜錯就多生一個 hex，累積到 89 個 hardcoded 顏色。`tr:hover` 居然用 `--sidebar-item-hover-bg`（語意錯）。
- **改法**：壓縮到 3 階 `--surface-1/-2/-3`（白、卡片次級灰、表頭灰），sidebar 變數別跨界用。
- **證據**：`style.css:7-30`, `style.css:2228`
- **工作量**：M

### 🟢 P2｜卡片間距節奏不一致（gap 1.5rem + margin 1rem + padding 各種 mix）

- **痛點**：眼睛感受不到「節奏」，整體看起來像拼貼。
- **改法**：定義空間 token（4/8/12/16/24/32），卡片 padding 一律 24px，view 內元素間距一律 16/24。
- **證據**：`style.css:2033, 2638, 1970`
- **工作量**：M

### ⚪ P3｜按鈕陰影過重 + hover translateY(-1px) 累積成「跳動感」

- **痛點**：dashboard 一頁 4 metric + 3 todo + 多顆 btn，滑鼠掃過頁面像在「呼吸」，閱讀文字被打斷。
- **改法**：只在主要 CTA 保留 translateY；metric-card 用「border 變色 + shadow 加深」就好。
- **證據**：`style.css:2138`, `style.css:623`
- **工作量**：S

### ⚪ P3｜check-banner 佔位過大（44×44 icon + 80px 高度），但 30 天只 5 天有內容

- **痛點**：每日 banner 大半時間空著，造成「banner blindness」，真的有事那天反而被忽略。
- **改法**：縮小 icon 到 24px、減少 padding；只有 15 號當天加微脈動 / 紅點；其餘日子整條淡化或隱藏。
- **證據**：`style.css:2828`
- **工作量**：S

---

## 五、無障礙與包容性

KI 的房東 / 會計多為 40-60 歲，視力、操作習慣都會影響使用。這章是「對所有人都更好」而不只是「對障礙者更好」。

### 🔴 P0｜Modal 完全沒有 ARIA 語意 + 無 focus trap

- **痛點**：所有 modal 缺 `role="dialog"` / `aria-modal` / `aria-labelledby`，Tab 可以走到 modal 後面去誤點 sidebar 連結（hash router 直接換頁，未存表單資料消失）。對螢幕報讀者完全不知道進入了對話框。
- **改法**：modal 加 ARIA 屬性、close 按鈕加 `aria-label`、打開時 body 設 `inert`、加 focus trap（Tab 在第一/最後個 focusable 之間 wrap）、close 後 focus 還回 trigger。
- **證據**：`js/utils/ui.js:18-61`
- **工作量**：M

### 🔴 P0｜LIFF 表單 label 沒有 `for`，房客視障場景不可用

- **痛點**：螢幕報讀者點到輸入框只報「文字框 必填」，不知道是「姓名」還是「手機」。LIFF 是 mobile screen reader 主場（聚空間是社會住宅性質，視障房客機率不低）。
- **改法**：每個 input 給 `id`，label 加 `for`；錯誤訊息用 `aria-describedby` 連結。
- **證據**：`liff/index.html:199-216`
- **工作量**：S

### 🟡 P1｜假按鈕（`div role="button"`）無 Enter/Space 鍵盤支援

- **痛點**：user-profile / sync-indicator / app-version 都是 div，純鍵盤使用者按 Enter 完全沒反應，只能用滑鼠。
- **改法**：直接改成 `<button type="button">`（自動拿到鍵盤支援）。
- **證據**：`index.html:111, 118, 135`
- **工作量**：S

### 🟡 P1｜表格內容對中年 KI 太小（th 0.75rem、occ-table 全 0.75rem、checkin-btn 0.65rem）

- **痛點**：很多房東 50+ 歲，每天看 450+ invoices。10-12px 中文遠低於 WCAG 舒適閱讀，會被迫 Ctrl+Plus 放大；但放大後 occ-table 跑版。
- **改法**：最小中文 13px (0.8125rem) 為底線；data-table th 去掉 uppercase（中文無效還更累）；住房一覽提供「字體縮放」三段切換。
- **證據**：`style.css:2188-2199, 1329-1333, 1298`
- **工作量**：M

### 🟡 P1｜sidebar muted 文字對比不過（`#9aa1ad` on 白 = 2.82:1）

- **痛點**：「總覽 / 營運 / 系統」三個分組標籤幾乎看不到，特別是辦公室螢幕反光下；KI 會覺得 nav 結構不清楚。
- **改法**：muted 色提到 `#6b7280` (4.83:1) 或 `#525965` (7+:1)；跑一次 axe DevTools 把所有 contrast warning 修掉。
- **證據**：`style.css:24, 57, 58`
- **工作量**：S

### 🟡 P1｜sidebar collapsed 模式 icon-only，無 tooltip 幾乎不能用

- **痛點**：桌機收合後變 76px 寬只剩 icon，6+ icon 排在一起，KI 必須記得「ph-wrench 是維修、ph-wallet 是帳務、ph-receipt 是…」第一次切換後幾乎都要展開才知道點哪個。
- **改法**：每個 nav-item 加 `title` / hover tooltip；折疊狀態下隱藏「物件管理 / 帳務管理」的展開 caret。
- **證據**：`sidebar.js:36-39`, `index.html:41-107`
- **工作量**：S

### 🟢 P2｜Focus 樣式被 11 處 `outline: none` 砍光，只剩部分元件有取代 ring

- **痛點**：純鍵盤使用者完全看不到自己 focus 在哪。`.form-input:focus` 雖有 border 變色但對色弱（紅綠盲很常見）幾乎看不出來。
- **改法**：全域 `*:focus-visible { outline: 2px solid var(--color-primary); outline-offset: 2px; }`；個別元件再覆蓋。
- **證據**：`style.css:293, 1082, 1529, 1857, 1918, 2119, 2601, 3408, 3543`
- **工作量**：S

### 🟢 P2｜全站 0 個 `prefers-reduced-motion` 支援

- **痛點**：modal 開關、sidebar 收合都有 transform 動畫。對前庭功能障礙 / 暈動症使用者每次切 view 都暈。WCAG 2.3.3 明確要求，且實作 1 行 media query 就完成。
- **改法**：加 `@media (prefers-reduced-motion: reduce)` 統一把動畫降到 0.01ms。
- **證據**：全站無命中
- **工作量**：S

### 🟢 P2｜topbar `/` 鍵盤提示是裝飾，沒實作快捷鍵

- **痛點**：顯示 kbd hint 等於承諾用戶「按 / 會 focus 搜尋」，按下去沒反應——信任受損。KI 養成「這系統的快捷鍵都是裝飾」的印象。
- **改法**：實作 `/` focus 搜尋、`Esc` 關閉搜尋面板、`↑↓ Enter` 在結果間導覽。
- **證據**：`index.html:156`, `topbar.js`
- **工作量**：S

### 🟢 P2｜icon-only 按鈕（通知 / kebab）缺 `aria-label`，只用 title

- **痛點**：title 在 mobile / 觸控完全消失；報讀者只聽到「按鈕」。
- **改法**：通知按鈕 `aria-label="通知（{n} 則未讀）"`，badge 內加 `aria-hidden="true"`。
- **證據**：`index.html:158-161`
- **工作量**：S

### ⚪ P3｜LIFF 提交按鈕無 loading aria-busy，spinner 無 `role="status"`

- **痛點**：視力差的房客提交後不知道「成功了沒」，可能重複送出。
- **改法**：spinner 加 `role="status" aria-live="polite"`；msg 容器加 `role="alert"`；提交時 button 加 `aria-busy="true"`。
- **證據**：`liff/index.html:105-128`
- **工作量**：S

### ⚪ P3｜麵包屑 / detail URL 缺失，重整或分享連結都失效

- **痛點**：在「合約管理 > 王大明合約 detail」開著時，重整瀏覽器就回到 list 頂端；想把問題合約 URL 傳給同事看，對方只看到 list 頁。
- **改法**：detail modal 開啟時 ID 寫入 URL hash（`#contracts/c-001`）；topbar 把 eyebrow 改成父分類顯示層級。
- **證據**：`app.js:50`，無 breadcrumb 實作
- **工作量**：M

---

## 六、手機體驗 / LIFF

系統雖然是 desktop-first，但「房客 LINE 來問 → 房東現場用手機開 BMS 查」是真實場景。

### 🟡 P1｜手機 / 平板上 sidebar 不會收，main content 被擠掉

- **痛點**：iPad mini 直立 768px 進來時，260px sidebar + topbar 佔掉 main 只剩 ~200px，住房一覽完全爛掉；iPhone 幾乎只看到 sidebar。
- **改法**：`@media (max-width: 900px)` sidebar 改 fixed + translateX(-100%)，topbar 加 hamburger；data-table 全部加 `overflow-x: auto`。
- **證據**：`style.css:87-96`（全寬度都 260px）
- **工作量**：M

### 🟡 P1｜form-grid 在 modal 內 640px 才切單欄，1366×768 上會擠

- **痛點**：modal max-width 600px，內部 form-grid 仍兩欄各 270px 還塞中文 label + input 會擠 / 截斷。住房一覽的 `.occ-table-wrap` 還是 `overflow: hidden`——右側欄根本看不到。
- **改法**：form-grid 切點提到 720px；occ-table-wrap 改 `overflow-x: auto`。
- **證據**：`style.css:3348, 1269`
- **工作量**：S

### 🟢 P2｜topbar 搜尋框 < 560px 直接 `display: none`，沒替代入口

- **痛點**：搜尋是 KI 找特定房客 / 合約唯一快速入口；手機進來變必須一路點 sidebar → 租客清單 → 拉到底找。
- **改法**：< 560px 改成 icon-only（放大鏡按鈕 → 點開叫出全螢幕搜尋 modal），不要直接砍掉入口。
- **證據**：`style.css:1960-1964`
- **工作量**：S

---

## 七、未來性 / 維運（次要但要記得）

### 🟢 P2｜通知中心與 dashboard 待辦來源不同步，可能不一致

- **痛點**：同一筆「待處理維修」會在 dashboard 待辦 + 通知中心徽章 + 房租查帳統計同時出現，處理一個地方另兩個還在顯示。且兩者邏輯來源不同（通知用 `c.status === '即將到期'`，dashboard 用 `getContractLifecycle`），可能出現 dashboard 顯示 3 筆但通知 5 筆的不一致。
- **改法**：抽出單一 `buildPendingActions()` 函式，全部呼叫它；通知加「已讀」狀態存 localStorage。
- **證據**：`topbar.js:155-197`, `dashboard.js:48-108`
- **工作量**：M

### 🟢 P2｜空狀態薄弱，新手房東第一天不知從哪開始

- **痛點**：「合約事項：暫無待辦」只是空白文字，沒引導下一步；settings 空狀態說「尚無資料」沒 CTA「新增第一個」。新手 KI 看到 dashboard 全空會懷疑「是不是壞了」。
- **改法**：標準化空狀態組件（icon + 一行說明 + 一個 CTA）；新接手 KI 加 onboarding checklist「① 加入第一個館 ② 加入床位 ③ 建立合約」。
- **證據**：`dashboard.js:289`, `analysis.js:184`, `settings.js:649`
- **工作量**：M

### 🟢 P2｜IA 寫死單一角色 admin，未來分權必須重整資訊結構

- **痛點**：未來會有「會計」「現場助理」分權，但現在 sidebar 11 個 menu item 全部對所有人開放。會計只需看帳務 4 子頁 + 報表，但 4 子頁散在 sidebar 無法整批 hide。
- **改法**：即使現在只有 admin，sidebar render 也走「按 role filter menu items」pipeline；user profile 加 role badge。配合第二章 P0 把帳務四子頁合一，會計 role 直接給整個帳務 menu 就行。
- **證據**：`Grep role` 只在 setup/supabase/login 出現
- **工作量**：M（含本章其他 finding 的話 L）

---

## 🎯 建議優先做的 5 件事（按 ROI 排）

| # | 改善項目 | 章節 | 工作量 | ROI 說明 |
|---|---|---|---|---|
| 1 | **帳務管理 4 子頁合併成單頁 + sub-tabs，命名統一** | 二 / P0 | L (2-3 天) | 影響 KI 最高頻的「查帳」工作流；命名混亂修一次受益永久；為未來「會計分權」鋪路 |
| 2 | **全域搜尋 + dashboard 待辦點擊直達 detail** | 一 / P0+P1 | M (半天-1天) | 改完後 KI 每天少按 30%+ 滑鼠；用戶感知最強烈的「變快了」 |
| 3 | **危險操作護欄套餐：dry-run preview + undo toast + 刪除阻擋有連動的合約** | 三 / P0+P0+P2 | M (1 天) | 直接消除「資料事故風險」；KI 心理安全感大幅提升，敢嘗試新功能 |
| 4 | **設計 token 整頓：對比修正 + 字級階梯 + focus 樣式 + tab 樣式收斂** | 四 / P0+P1+P2 (合併做) | M (1-1.5 天) | 一次性把視覺基底打穩，後續所有新 feature 自動更一致；WCAG AA 一次通過 |
| 5 | **新增入住改 3 步 wizard + 加「儲存並繼續新增」** | 二 / P0 + 三 / P1 | L (2 天) | 每次入住節省 1-2 分鐘 × 每月十幾筆；新手 KI 不會在表單裡迷路 |

做完這 5 件，53 條 finding 裡的 5 個 P0 全清掉，10 個 P1 順帶處理掉。**預估總工期 6-8 個工作天**。

---

## 💡 設計師說的話

KI 老闆好，

整體看下來，這套 BMS 的「**骨架很穩**」——sidebar、topbar、卡片、modal、表單元件都有用心做過，配色暖橘有辨識度，看得出設計師有審美。問題集中在三個地方：

**第一，視覺優先序沒做出來。** 你每天打開 dashboard 想看的是「今天該做什麼」，但目前四張 metric 卡權重相同、待辦點下去只跳列表頁、搜尋找到結果也不會直接打開。這些都是「**讓系統替你思考**」的關鍵改動——做完之後你每天會少按 30% 的滑鼠，這是用戶感知最強烈的進步。

**第二，危險操作沒有護欄。** 結帳、退租、補產帳單、刪除合約這些「不可逆 + 連動」操作目前都是 fire-and-forget。加一個 5 秒的 undo toast + dry-run preview，事故率會直線下降。MEMORY 裡記載你被覆蓋過 3 次資料——這就是缺護欄的代價。

**第三，帳務四個子頁該合併。** 「總收支表 / 收支分析 / 房租查帳 / 各館收入報表」這四個並列頁面是 IA 最大的痛點，名稱重疊、職責重疊、命名一個概念三個名字。合併成「帳務管理」單頁 + sub-tabs，配上共用月份切換器，工作流會順非常多。同時也為未來「會計角色」鋪路——直接把整個帳務 menu 給會計就好。

**我的建議是：先做「優先 5 件事」的前 3 件**（帳務合併 + 搜尋直達 + 危險操作護欄），你跟同事就會立刻有感「系統變聰明了」。視覺 token 整頓跟入住 wizard 排在後面做，因為那兩個是「**長期受益但短期無感**」型的投資。

無障礙的部分（Modal ARIA、LIFF label、focus 樣式）建議跟視覺 token 一起做，因為都是 CSS / 結構層級的全域改動，一次處理摩擦成本最低。

最後一點：**現在 491 處 inline style 是個警訊**。它不會今天爆掉，但每加一個新 view 就會更難維護，最終某天會卡住「想做 dark mode 卻發現要改 500 個地方」。這個技術債建議排在優先 5 件事之後馬上處理。

整份報告 38 個項目，全做完大概 3-4 週工程量。但**只要前 5 件做完，KI 的日常痛感應該降低 70%**。剩下的 33 個可以當 backlog 慢慢做，不急。

期待跟你討論優先順序。

— 設計團隊
